version: "3.7"
services:
  app:
    build:
      context: ./main
      dockerfile: Dockerfile.main
    ports:
      - 5000:5000

  # sub_app:
  #   build:
  #     context: ./sub
  #     dockerfile: Dockerfile.sub_app
  #   ports:
  #     - 5001:5001

  nginx:
    container_name: nginx1
    build:
      context: ./nginx
      dockerfile: Dockerfile.nginx
    restart: always
    ports:
      - "80:80"
